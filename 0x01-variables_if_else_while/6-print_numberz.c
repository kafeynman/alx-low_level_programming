#include <stdio.h>

/**
 * main - entry point of program
 * Description: Prints base 10 numbers
 * Return: 0 (successful), 1 (otherwise)
 */
int main(void)
{

	for (int i = 48; i < 58; i++)
	{
		putchar(i);
	}
	putchar('\n');

	return (0);

}
